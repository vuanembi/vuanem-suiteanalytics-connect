SELECT
    LEAST(
        {% for key in incre_key -%}
            MAX({{ key }})
            {% if not loop.last -%}
                ,
            {%- endif %}
        {%- endfor %}
        ) AS incre
    FROM
        `{{ dataset }}`.`_stage_{{ table }}`
